#!/bin/sh
# Description: Re-start Google Chrome in Dark Mode
UNAME=$(uname -s)
case "$UNAME" in
	Linux) CHROME_NAME=chromium ;;
	OpenBSD) CHROME_NAME=chrome ;;
	*) echo default
	;;
esac

pkill $CHROME_NAME
$CHROME_NAME --force-dark-mode $*
